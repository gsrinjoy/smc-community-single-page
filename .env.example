# Meta (Facebook) Pixel.
# The live ID is committed in .env — copy this to .env.local only if you need
# to override it (e.g. point a staging build at a different pixel).
# Find it in Events Manager -> Data Sources -> your pixel (15-16 digits).
VITE_META_PIXEL_ID=
